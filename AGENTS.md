# Code Style & Quality

All Python code must adhere to the Ruff formatter standards.
Before submitting any changes or creating a Pull Request, you MUST:
1. Run `ruff format .` to fix formatting.
2. Run `ruff format --check` to verify compliance.
3. Run `ruff check` to catch linting errors.

If formatting fails, correct it before proceeding.
