// Re-export affix metadata from the shared package (single source of truth).
export {
  STATIC_AFFIXES,
  BARGAIN_AFFIXES,
  resolveAffixDisplay,
} from '@mythicplus/shared';

export type { AffixDisplay } from '@mythicplus/shared';
