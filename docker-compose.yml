version: '3.8'

services:
  bot:
    image: ${IMAGE_NAME:-mythic-plus-bot}:${IMAGE_TAG:-latest}
    build: .
    container_name: mythic-plus-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DISCORD_APPLICATION_ID=${DISCORD_APPLICATION_ID}
      - PREFERENCES_PATH=/data/player_preferences.json
    volumes:
      - bot-data:/data
    restart: unless-stopped

volumes:
  bot-data:
