--- activity/src/wheel.ts
+++ activity/src/wheel.ts
@@ -8,6 +8,13 @@
   '#e91e63', '#00bcd4', '#8bc34a', '#ff9800',
 ];

+const PRECOMPUTED_COLORS = COLORS.map(color => ({
+  base: color,
+  lightened: lighten(color, 0.2),
+  darkened: darken(color, 0.15),
+  desaturated: desaturate(color)
+}));
+
 function desaturate(hex: string, amount = 0.6): string {
   const r = parseInt(hex.slice(1, 3), 16);
   const g = parseInt(hex.slice(3, 5), 16);
@@ -206,12 +213,12 @@
     this.entries.forEach((entry, i) => {
       const startAngle = this.rotation + i * sliceAngle;
       const endAngle = startAngle + sliceAngle;
-      const color = COLORS[i % COLORS.length];
+      const colorSet = PRECOMPUTED_COLORS[i % PRECOMPUTED_COLORS.length];

       // Segment fill with radial gradient
       const grad = this.ctx.createRadialGradient(cx, cy, radius * 0.15, cx, cy, radius);
-      grad.addColorStop(0, lighten(color, 0.2));
-      grad.addColorStop(1, entry.isOffspec ? desaturate(color) : darken(color, 0.15));
+      grad.addColorStop(0, colorSet.lightened);
+      grad.addColorStop(1, entry.isOffspec ? colorSet.desaturated : colorSet.darkened);

       this.ctx.beginPath();
       this.ctx.moveTo(cx, cy);
